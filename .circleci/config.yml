version: 2.1
jobs:
  build:
    docker:
      - image: node:10
    steps:
      - run:
          name: "Build Stage"
          command: "docker build -t someimage -f Dockerfile.dev ."
workflows:
  say-hello-workflow:
    jobs:
      - build
